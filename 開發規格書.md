# P_POC 開發規格書（實作紀錄與異動說明）

本文件記錄實作過程中相對於《P_POC_開放式測試招募平台_開發規格書_FirebaseNative.docx》
（以下稱「原規格書」）的架構異動、Bug 修復、新增功能與部署現況。原規格書仍為業務流程與
資料綱要的權威依據，本文件僅記錄「實作時發現需要調整」與「原規格書之外新增」的部分。

最後更新：2026-09-07

---

## 1. 架構層級異動：Cloud Functions → 平台無關 Node/Express 後端

**原規格書設計**：後端邏輯以 Firebase Cloud Functions（`asia-east1`）實作，前端以
`httpsCallable()` 呼叫。

**實際遇到的問題**：本專案 GCP 專案所屬組織（`bw-t001317-org`）啟用了
**Domain Restricted Sharing** 組織政策，禁止將 `allUsers` / `allAuthenticatedUsers`
設為任何資源（含 Cloud Run / Cloud Functions 2nd Gen 底層服務）的存取主體。Firebase
Callable Functions 的標準運作方式需要 Cloud Run 服務對外公開（讓請求先通過網路層，
再由函式內部檢查 Firebase ID Token），因此在此組織政策下**完全無法呼叫**，前端出現
CORS 預檢失敗 + `functions/internal` 錯誤。嘗試在專案層級覆寫該政策亦被 Console
擋下（`無法將「allUsers」和「allAuthenticatedUsers」類型的主體新增至這項資源`）。

**解法**：改在 [`server/`](server/) 目錄實作一支**平台無關**的 Node.js + Express API
伺服器，取代原本的 5 支 Cloud Functions，業務邏輯完全相同（含 Firestore Transaction
排他鎖、庫存檢查），僅將觸發方式從 `onCall` 改為一般 HTTP 路由：

| 原 Cloud Function | 對應 Express 路由 |
|---|---|
| `sendInvites` | `POST /api/sendInvites` |
| `approveAndAssignReward` | `POST /api/approveAndAssignReward` |
| `getRewardSerial` | `POST /api/getRewardSerial` |
| `addRewardSerials` | `POST /api/addRewardSerials` |
| `getRewardStock` | `GET /api/getRewardStock` |

身分驗證改由 [`server/middleware.js`](server/middleware.js) 手動驗證前端傳入的
`Authorization: Bearer <Firebase ID Token>`（`admin.auth().verifyIdToken()`），
安全性等同原本 Cloud Functions 的 `request.auth`。**Firestore 安全性規則完全不需要
更動**——`rewards` 集合維持「前端全閉鎖，只能由持有服務帳戶憑證的後端存取」，只是
「後端」從 Cloud Functions 換成這支 Express 伺服器。

此架構的好處是**不綁定任何雲端平台**：可部署於任何支援 Node.js 的環境（VPS、Render、
Railway、Fly.io、Docker 容器...），與前端未來要換到 Netlify / Vercel 等平台完全無關。

`functions/` 目錄與 `firebase.json` 內的 `functions` 設定區塊已移除。

---

## 2. Bug 修復紀錄

### 2.1 Firestore 規則：Admin 無法列出全部名單

**原規格書規則**（[`firestore.rules`](firestore.rules) 第 6 節逐字照抄）僅允許：
```
allow read, create: if request.auth != null && request.auth.uid == uid;
```
這代表**每個使用者只能讀取自己的 `applicants`/`submissions` 文件**，但後台
「意願登記審核」「回饋審核與發券」頁面需要 `onSnapshot` 監聽整個 collection——
admin 帳號登入後台一樣被 `permission-denied` 擋下，畫面空白。

**修正**：將 `applicants/{uid}` 與 `submissions/{uid}` 的 `read` 規則改為
「本人 **或** 具備 `admin` custom claim」皆可讀取整個 collection：
```
allow read: if request.auth != null &&
  (request.auth.uid == uid || request.auth.token.admin == true);
```

### 2.2 Firestore 複合索引未部署

`questions` 集合查詢條件為 `where("isActive","==",true).orderBy("sortOrder","asc")`，
需要複合索引，但 `firestore.indexes.json` 從未實際 `firebase deploy --only firestore:indexes`
過。結果是 `test.html` 進入問卷頁面後題目清單永遠空白（`FirebaseError: The query
requires an index`，被 `await` 但未被外層 catch，導致後續渲染流程整個中斷）。已部署索引。

### 2.3 Google 登入無法切換帳號

`signInWithPopup` 預設會沿用瀏覽器既有的 Google 登入工作階段，若使用者之前已用某帳號
登入過 Google，點擊「使用 Google 帳號登入」不會顯示帳號選擇畫面，而是直接用同一帳號
靜默登入——造成「登出後重新登入還是同一個人」的錯覺。

修正：[`assets/js/firebase-config.js`](assets/js/firebase-config.js) 對
`googleProvider` 加上 `setCustomParameters({ prompt: "select_account" })`，強制每次
都顯示帳號選擇畫面。

### 2.4 `rewards.status` 從未轉為 `REDEEMED`

`getRewardSerial`（受測者領取序號時）只更新 `redeemedAt` 時間戳，未同步把
`status` 從 `ASSIGNED` 改為 `REDEEMED`。後果：後台「獎勵序號池」統計中的
「已兌換」數量永遠顯示 0，即使實際上已有人完成兌換。已修正為同時寫入
`status: "REDEEMED"`。

### 2.5 新增題目時 sortOrder 衝突

後台新增題目若指定一個已存在的 `sortOrder`（例如已有題目為 1，新增時又填 1），
原本只是單純寫入相同數字，兩題順序變成不明確（Firestore 對相同排序值的次序不保證
穩定）。修正 [`assets/js/admin.js`](assets/js/admin.js) 的新增題目邏輯：新增前先
查詢 `sortOrder >= 指定值` 的題目，批次將其 `sortOrder` 全部 +1，再寫入新題目，
達成「插入指定位置、其餘自動往後遞補」的直覺行為。

---

## 3. 原規格書隱含、但程式碼範例未列出的必要 API

`rewards` 集合安全性規則為 `allow read, write: if false`（前端完全鎖死，即使
admin 帳號也一樣），但規格書的業務流程明確要求「Page 4 序號兌換」與「後台獎勵序號
池管理」兩項功能，因此新增了 3 支規格書程式碼範例未列出的 API（現為 Express 路由，
邏輯與命名沿用自最初以 Cloud Functions 實作的版本）：

- `getRewardSerial`：受測者於 `redeem.html` 查詢自己被指派的序號
- `addRewardSerials`：後台批次新增獎勵序號
- `getRewardStock`：後台查詢庫存統計（可用/已指派/已兌換 各幾組）
- `getRewardList`（本輪新增，見第 4 節）：後台查詢完整序號明細清單

---

## 4. 本輪新增功能

### 4.1 獎勵序號池「序號明細清單」

後台原本只顯示庫存數量統計，看不出「哪組序號發給了誰」。新增
`GET /api/getRewardList`（[`server/index.js`](server/index.js)），伺服器端
join `rewards` 與 `applicants` 兩個 collection，回傳含指派對象姓名/Email 的完整
清單；`admin.html` / `admin.js` 新增「序號明細清單」表格呈現。

### 4.2 通知信內容加入頁面連結

原本的邀請信、發券信只有文字說明，沒有實際連結。新增環境變數 `FRONTEND_URL`
（見 `server/.env.example`），信件內容改為：
- 邀請信 → `${FRONTEND_URL}/test.html`
- 發券信 → `${FRONTEND_URL}/redeem.html`

### 4.3 `test.html` 已核准狀態顯示兌換頁連結

原本受測者若已完成受測且審核通過，頁面只顯示純文字「請至禮券兌換頁面領取獎勵」，
沒有可點擊的連結。已改為超連結直接導向 `redeem.html`。

### 4.4 `redeem.html` 新增 QR Code

原本只有 CODE128 條碼（JsBarcode）。新增 QR Code（與條碼編碼相同的序號字串），
方便超商端以手機掃描或條碼機讀取。

**技術選型過程**：一開始選用 npm 套件 `qrcode@1.5.3` 的 CDN 建置檔，但該版本
發布的 `lib/browser.min.js` 是 **CommonJS 格式**（內含 `require(...)`），並非
瀏覽器可直接以 `<script>` 載入的 UMD/IIFE 建置檔，載入後會靜默失敗（`window.QRCode`
為 `undefined`）。改用 `qrcodejs`（cdnjs，`davidshimjs/qrcodejs`，即規格書
3.2 節提及的「qrcode.js」），這套件本身就是為瀏覽器 `<script>` 標籤直接使用而設計。

---

## 5. 環境變數與部署設定（`server/`）

見 [`server/.env.example`](server/.env.example) 完整清單，重點：

| 變數 | 用途 |
|---|---|
| `GOOGLE_APPLICATION_CREDENTIALS` / `GOOGLE_APPLICATION_CREDENTIALS_JSON` | Firebase Admin SDK 服務帳戶憑證（本機用檔案路徑，正式託管平台用 JSON 字串環境變數） |
| `ALLOWED_ORIGINS` | CORS 允許的前端網域（逗號分隔） |
| `FRONTEND_URL` | 通知信中連結所指向的前端網址 |
| `MAIL_HOST` / `MAIL_PORT` / `MAIL_USER` / `MAIL_PASS` / `MAIL_FROM` | SMTP 寄信設定 |

### 5.1 Email 寄送（SendGrid）

實際測試採用 SendGrid SMTP Relay（`smtp.sendgrid.net:587`，帳號固定為
`apikey`，密碼為 SendGrid API Key）。**SendGrid 要求 `MAIL_FROM` 必須是已完成
Sender Identity 驗證的信箱**（Settings → Sender Authentication → Verify a Single
Sender），否則寄信會被拒絕（`550 The from address does not match a verified
Sender Identity`）；驗證後仍可能有數分鐘的生效延遲。目前已驗證信箱：
`asbe2026@gmail.com`。

### 5.2 管理員身分設定

`admin.html` 透過 Firebase Auth custom claim `admin: true` 判斷權限，此聲明
**不透過任何可呼叫 API 設定**，只能用 [`scripts/set-admin-claim.js`](scripts/set-admin-claim.js)
搭配服務帳戶金鑰在本機執行一次性設定。目前已設定 `st.90506335@gmail.com` 為管理員。

---

## 6. 題庫多元題型（2026-09-07 新增）

原規格書 `questions` 集合僅支援單一種題型（純文字多行 Textarea）。已擴充為 6 種題型，
`questions/{id}` 新增 `type` 欄位（缺省視為 `TEXT`，向下相容既有題目）：

| `type` 值 | 說明 | 額外欄位 |
|---|---|---|
| `TEXT`（預設） | 多行文字（原本唯一支援的題型） | 無 |
| `SINGLE_CHOICE` | 單選 | `options: string[]`（至少 2 個） |
| `MULTIPLE_CHOICE` | 多選 | `options: string[]`（至少 2 個） |
| `YES_NO` | 是非題 | 無（固定「是」/「否」） |
| `RATING` | 評星 | `ratingMax: number`（預設 5） |
| `SLIDER` | 數值滑桿 | `sliderMin` / `sliderMax` / `sliderStep`（預設 0/10/1） |

**後台**（`admin.html` / `admin.js`）新增題目時可選擇題型，並依題型顯示對應設定欄位
（選項清單／星星上限／滑桿範圍），題目列表新增「題型」欄。

**前端問卷**（`test-flow.js`）依 `type` 動態渲染對應的輸入元件（radio / checkbox /
星星按鈕 / `<input type="range">`），並各自實作必填驗證（單選/是非題須選取、多選須至少
勾 1 項、評星須點選星星；滑桿因一定有預設數值故視為必填已滿足）。

**答案儲存格式**：`submissions.answers[]` 每筆新增 `questionType` 與 `answerValue`
（結構化原始值：字串/數字/布林/陣列），並保留 `answerText`（給後台審核頁面顯示用的
可讀文字，例如評星存成 `"4 / 5 顆星"`、多選存成以「、」分隔的字串），後台審核畫面
完全不需改動即可正確顯示所有題型的回答。

已用「單選＋評星＋滑桿＋是非題＋多選＋文字」混合題型完整測試過一輪，含驗證邏輯、
資料儲存格式、後台審核顯示，全部正常運作。

### 6.1 單選／多選新增「其他（請說明）」選項

僅適用於 `SINGLE_CHOICE` 與 `MULTIPLE_CHOICE`，題目新增 `allowOther: boolean`
欄位（預設關閉，後台新增題目時勾選「允許『其他（請說明）』自由回答」才會啟用）。
啟用後，前端問卷在選項清單最後多顯示一個「其他：____」輸入列：

- 單選：選了「其他」時，實際答案是使用者輸入的自由文字（`answerValue` 存文字本身，
  `answerText` 存 `"其他：<文字>"` 供後台顯示區分），若勾選但未填文字視為未作答。
- 多選：「其他」是清單中普通的一個選項，可以跟其他固定選項一起複選；若勾選但
  未填文字，同樣視為未作答阻擋送出。

### 6.2 題庫列表 UI 優化：設定內容顯示 + 就地修改

**選項/設定內容顯示**：題型欄原本只顯示題型名稱（如「單選」），看不出實際填了
什麼選項。改為：
- 每題下方新增一列縮排的說明文字（例如「選項：電話、面談、線上問卷（＋其他）」
  、「最多 5 顆星」、「範圍 0 ~ 10（間距 1）」），一目了然不用額外互動
- 題型文字同時保留虛線底線 + `title` 滑鼠提示（兩種呈現方式並存）

**就地修改（inline edit）**：新增「修改」按鈕，點擊後原本的縮排說明列會直接
變成可編輯表單（題目文字、題型、選項/設定、順序），改完點「儲存」寫回 Firestore，
不需要捲動到頁面上方的「新增題目」表單。以 `editingQuestionIds`（Set）追蹤目前
處於編輯狀態的題目 id，搭配 `lastQuestionsDocs` 快取住最新一次 `onSnapshot` 的
文件清單，「取消」可以不必重新讀取 Firestore、直接用快取重繪列表。

**除錯註記**：實作時發現「儲存」後編輯表單沒有自動收合——因為 `updateDoc` 觸發
的 `onSnapshot` 重繪，與程式碼自己把 id 從 `editingQuestionIds` 移除的時序有
競態（`onSnapshot` 可能先於程式碼執行完成前就先重繪一次），導致最後一次真正該
收合表單的重繪從未被觸發。修正方式是在 `editingQuestionIds.delete(id)` 之後
明確呼叫一次 `renderQuestionsTable(lastQuestionsDocs)`，不依賴 `onSnapshot`
之後才重繪。

### 6.3 開發測試期間發現的瀏覽器快取問題（修正：4 個頁面的 script 加上版本號）

本機測試時多次遇到改了 `assets/js/*.js` 之後，瀏覽器（含一般重新整理、甚至改用
不同分頁）仍執行舊版程式碼——最嚴重的一次連 `admin.html` 這個 HTML 文件本身都
被快取住，重新整理無效，只有在網址加上快取破壞用的查詢參數（如 `?t=123`）才會
真正重新從伺服器取得。本機用 Python `http.server` 沒有送出任何 `Cache-Control`
標頭，瀏覽器會用啟發式規則自行決定快取多久，是主要成因；正式站（GitHub Pages）
理論上有較明確的快取行為，但同一類問題（使用者瀏覽器快取住舊版 JS）在正式環境
也可能發生。因此為 4 個頁面（`index.html` / `admin.html` / `test.html` /
`redeem.html`）各自的進入點 `<script type="module">` 標籤都加上版本查詢字串
（例如 `admin.js?v=20260907a`），未來每次異動這些 JS 檔案時記得手動遞增版本號，
確保使用者瀏覽器一定會抓到最新版本。

### 6.4 修改題目時的順序衝突未遞補（沿用 2.5 的邏輯到「修改」）

[2.5](#) 修的是「新增」題目時的順序衝突，但「修改」既有題目、把順序改成與別題
相同時，並沒有套用同一套遞補邏輯——單純直接覆寫，導致兩題順序相同。已補上：
儲存修改時，用 `writeBatch` 一次處理「把目標順序（含）之後、且不是自己的題目
都 +1」與「寫入自己最終的異動內容」，跟新增流程使用相同邏輯，只是多排除了
「自己」這個文件，避免把正在編輯的題目自己也錯誤地往後推一位。

### 6.5 自訂網域 `poc.ash-ben.com` 未加入 Firebase Auth 授權網域

自訂網域上線後，Google 登入完全沒有反應（連登入視窗都不會跳出來）——原因與
[2.3](#) 類似：`signInWithPopup` 在跳窗前會先檢查目前網域是否在 Firebase Auth
的授權網域清單中，`poc.ash-ben.com` 從未被加入過（清單原本只有 `localhost`、
`*.firebaseapp.com`、`*.web.app`、`st90506335-source.github.io`），因此
`auth/unauthorized-domain` 直接被 catch 吃掉、畫面上完全無感。已透過
Identity Toolkit Admin API（`PATCH .../admin/v2/projects/{id}/config`）
將 `poc.ash-ben.com` 加入 `authorizedDomains`；這項設定不受先前 Cloud Run
「Domain Restricted Sharing」組織政策影響，服務帳戶本身的 `roles/editor`
即有權限直接修改，不需要走 Console 手動設定。

### 6.6 修正「修改題目」導致順序集體亂跳的 bug，改用 ▲▼ 重新排序取代手動輸入順序值

[6.4](#) 的修法本身是錯的：把「新增」用的『插入並整批往後遞補』邏輯直接套用到
「修改」，變成**只要點修改再儲存，不管有沒有真的改順序，一律把該題（含）之後
的所有題目順序全部 +1**——因為修改表單開啟時「順序」欄位預設帶入的就是這題
原本的值，等於每次儲存都觸發一次全體遞補，越改越亂，這就是回報的「排序一直
亂跳」。已將「修改」的儲存邏輯改回單純寫入使用者輸入的順序值，不再有任何自動
遞補副作用。

同時新增更好的重新排序方式：每題順序數字旁加上 ▲▼ 按鈕，點擊時與畫面上相鄰的
題目「交換位置」，並依交換後的畫面順序把全部題目重新編為連續整數（1, 2, 3...），
一次操作就能順便清掉過去累積的跳號/重複號（`moveQuestion()`）。「新增」題目
時插入指定順序的遞補邏輯（[2.5](#)）維持不變，只有「修改」和「重新排序」的
行為調整。

### 6.7 導入全站視覺風格系統（暖陽／質簡／森系／柔櫻 4 組可切換主題）

新導入一套 CSS 變數驅動的視覺設計系統：4 組配色主題、玻璃擬態卡片、動態光暈
背景、按鈕光澤掃過動畫、自訂 checkbox/radio 樣式。先套用到 **`test.html`**
（問卷頁），確認與既有 JS 邏輯相容後，再套用到 **`index.html`**（意願登記頁，
與 `page0.js` 100% 相容，沿用一模一樣的元素 id，直接整份替換即可，`page0.js`
完全沒有異動）。

套用到 `test.html` 時對 `test-flow.js` 做了幾處小調整以配合新樣式，行為完全不變：
- `goToStep()` 原本直接切換寫死的 Tailwind indigo 色系 class，改成切換單一
  `active` class，實際顏色交由 CSS 變數 `--primary-gradient` 決定，才能讓
  步驟指示點跟著主題切換即時變色
- 單選/多選的 `<input>` 從陽春的 `w-4 h-4` 換成 `custom-radio` /
  `custom-checkbox`（`other-radio` / `other-checkbox` 這兩個作為 JS 判斷用的
  標記 class 維持不變，兩者並存）
- 原設計系統只定義了方形 `.custom-checkbox`，補上一組對應的圓形
  `.custom-radio`（含選中時的圓點動畫），單選題才不會套用方形打勾樣式

主題選擇存在 `localStorage`（key: `p_poc_theme`），`index.html` 與 `test.html`
共用同一把 key，使用者在任一頁面切換主題，跳到另一頁時會自動套用相同主題。
`admin.html` / `redeem.html` 目前尚未套用此風格系統，維持原本樣式。

### 6.8 題目必填設定（含條件式必填/隱藏）

使用者原提出 3 種必填形式：① 直接勾此題必填、② 哪一題選「是」則此題必填（否則
隱藏）、③ 條件式必填（否則隱藏）。討論後確認 ② 其實是 ③ 的特例（觸發題限定
是非題、觸發值限定「是」），因此收斂成一套統一機制，而非分開實作三套邏輯。

**最終資料模型**（`questions/{id}`）：
- `requiredMode`: `"ALWAYS"`（一律必填，**預設值**，依使用者指示）｜
  `"OPTIONAL"`（一律非必填）｜`"CONDITIONAL"`（條件式）
- `condition`（僅 `requiredMode === "CONDITIONAL"` 時有值）：
  `{ questionId, operator: "eq"|"gt"|"lt", value }`——觸發題目 id、比較運算子、
  比對值。`eq` 用字串比較（陣列型答案如多選則用 `includes`），`gt`/`lt` 用
  數值比較，兩邊轉不成數字時視為不成立。

**行為**：`CONDITIONAL` 題目預設隱藏，前端問卷會監聽整個題目容器的
`change`/`input` 事件（含星等/滑桿的手動觸發），即時重新評估每一條條件式題目
是否該顯示；條件不成立時該題完全隱藏且不計入必填驗證與送出資料，成立時該題
視同 `ALWAYS` 必填。已用真實題目「願意接受訪問嗎？（是非題）→ 可接受訪問的
聯繫方式（多選，條件：上題等於「是」）」完整測試：選「否」時下一題正確隱藏
且送出時完全不出現在 `answers[]`；選「是」但不填則正確擋下送出；雙向切換也
即時正確顯示/隱藏。

**這次討論中提出但決定先不做、留待之後有需求再評估的形式**：
- **多條件同時成立（AND/OR）**：例如「Q1=是 且 Q2=否」才顯示某題。目前
  `condition` 只支援單一觸發題，之後如果需要，資料結構會需要改成
  `conditions: [...]` 陣列並定義如何組合。
- **同時定義多個必填模式規則堆疊**：目前一題只能有一組 `condition`，若要
  「條件 A 才顯示，但condition B才必填」這種顯示與必填分離的更細緻控制，
  現在的機制沒有涵蓋。

**已經涵蓋、不需要另外補充的部分**：使用者最初的 #2（是非題觸發）已經是 #3
（任意題型觸發）的資料結構完全相容子集，`condition.questionId` 可以指向任何
題型的題目，`operator` 也支援數值型的大於/小於（例如「滿意度評分 < 3 分才
追問原因」），這些在設計討論時提出、且已經一併實作完成，不是只做了是非題的
特例版本。

---

## 7. 已知待辦事項

1. **`server/` 尚未部署到正式環境**——目前僅在本機以 `localhost:8081` 測試，
   `assets/js/api-config.js` 的 `API_BASE_URL` 仍指向 localhost，正式上線前需：
   - 選定部署平台（Render / Railway / Fly.io / VPS / Docker...）
   - 將服務帳戶憑證改為該平台的環境變數（`GOOGLE_APPLICATION_CREDENTIALS_JSON`），
     **不要**把金鑰檔案本身上傳過去
   - 更新 `API_BASE_URL` 與 `server` 端的 `ALLOWED_ORIGINS` / `FRONTEND_URL`
2. **`settings/config` 尚未透過後台填寫**——`index.html`／`test.html` 目前顯示的
   `appName`／`featureIntro`／手冊連結都是程式內建的預設文字，需要在後台「系統設定」
   分頁實際填寫並儲存一次。
3. **服務帳戶金鑰檔案目前仍留在專案資料夾內**（已加入 `.gitignore`，不會被提交），
   待正式部署完成、確認不再需要本機測試後，應刪除該檔案並到 Firebase Console
   撤銷這把金鑰。
4. Cloud Functions 版本的舊資源（GCP 上曾經部署過的 5 支函式）目前仍存在於
   Firebase 專案中（未被實際使用），可考慮之後清理以避免佔用資源／混淆。
